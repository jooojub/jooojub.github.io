{"pageProps":{"fileName":"2026-02-19-post-bash_RANDOM_variable","title":"Bash $RANDOM Variable","cover":"/assets/posts/linux/post-bash_RANDOM_variable.jpg","tags":["bash"],"date":"2026-02-19","review":"A detailed explanation of how to use the Bash $RANDOM built-in variable to generate random numbers between 0 and 32767, including how to specify ranges, set seeds, and an analysis of its internal operational principles based on open-source code.","description":"Understanding and using the $RANDOM internal variable in Bash, including its open-source implementation","series":"linux","type":"posts","postKey":"0bdfcc0a20c464ea5d4d7218d74d45f5","content":"\n## Bash $RANDOM Variable\nIn `bash`, `$RANDOM` is a built-in variable that returns an integer between <mark>0 and 32767</mark> each time it's called.  \nWhen you need a simple random number, it can be used quickly without any external utilities.\n### Basic Usage\nThe most basic usage is simply referencing the variable.\n```sh\n{\n    \"title\": \"Generate random number\"\n}\n---\n$ echo $RANDOM\n14382\n```\nTo get a number within a specific range (e.g., 1-100), use the <mark>modulo operator (%)</mark>.\n```sh\n{\n    \"title\": \"Random number in range 1-100\"\n}\n---\n$ echo $(( (RANDOM % 100) + 1 ))\n```\n### Seeding RANDOM\nAssigning a value to the `RANDOM` variable sets an <mark>integer seed</mark>.  \nProviding the same seed will result in the same random sequence every time.\n```sh\n{\n    \"title\": \"Setting a seed\"\n}\n---\n$ RANDOM=42\n$ echo $RANDOM\n14194\n```\n### Deep Dive: Bash Open Source Code\nLet's take a look at how `$RANDOM` is implemented internally in Bash.  \nLooking at the Bash source code (`variables.c`), this variable is treated not as a simple variable, but as a <mark>dynamic variable</mark>.\n\n### 1. Dynamic Variable Registration\nDuring the Bash initialization process, the `RANDOM` variable is registered with special handlers via the `initialize_dynamic_variables()` function.\n```c\n{\n    \"title\": \"variables.c - initialize_dynamic_variables\"\n}\n---\nstatic void\ninitialize_dynamic_variables (void)\n{\n  SHELL_VAR *v;\n  ...\n  /* Bind the get_random read function and assign_random write function to the RANDOM variable */\n  // [!code highlight]\n  INIT_DYNAMIC_VAR (\"RANDOM\", (char *)NULL, get_random, assign_random);\n  VSETATTR (v, att_integer); // Assign integer attribute\n}\n```\n<AdCard></AdCard>\n### 2. Generating Random Numbers\nWhen a user calls `$RANDOM`, it executes in the order `get_random()` -> `get_random_number()`, and the actual random number generation calls the `brand()` function (an internal LCG algorithm).\n```c\n{\n    \"title\": \"variables.c - get_random_number\"\n}\n---\nint\nget_random_number (void)\n{\n  int rv, pid;\n\n  /* Reset for command and process substitution. */\n  pid = getpid ();\n  if (subshell_environment && seeded_subshell != pid)\n    {\n    //[!code highlight]\n      seedrand (); // Reseed based on new PID\n      seeded_subshell = pid;\n    }\n\n  /* Generate random numbers until they don't duplicate the previous value */\n  do\n  // [!code highlight]\n    rv = brand ();\n  while (rv == last_random_value);\n\n  return (last_random_value = rv);\n}\n```\n### 3. Seeding via Assignment\nWhen a user assigns a value, such as `RANDOM=42`, the `assign_random` function is called to initialize the random number generator's seed.\n```c\n{\n    \"title\": \"variables.c - assign_random\"\n}\n---\nstatic SHELL_VAR *\nassign_random (SHELL_VAR *self, char *value, arrayind_t unused, char *key)\n{\n  intmax_t seedval;\n  int expok;\n\n  /* Evaluate if the input value is a number */\n  if (integer_p (self))\n    seedval = evalexp (value, 0, &expok);\n  else\n    expok = valid_number (value, &seedval);\n\n  if (expok == 0)\n    return (self);\n\n  /* Set the internal generator's seed via sbrand */\n  // [!code highlight]\n  sbrand (seedval);\n  \n  if (subshell_environment)\n    seeded_subshell = getpid ();\n\n  /* Update the integer value set on the variable itself */\n  return (set_int_value (self, seedval, integer_p (self) != 0));\n}\n```\n### Key Implementation Details\n* `LCG (Linear Congruential Generator):` Bash uses the lightweight <mark>brand()</mark> function for performance.  \nIts deterministic nature means the same seed will yield the same results.\n* <mark>Subshell Handling:</mark> To prevent subshells (like `$(echo $RANDOM)`) from producing the same random sequence as their parent, the source code checks `getpid()` and automatically performs `seedrand()` if the session differs.\n* <mark>att_integer:</mark> `$RANDOM` is assigned an integer attribute at the time of creation, minimizing type conversion overhead during arithmetic operations.\n* The range of `$RANDOM` is from 0 to 32767 (2^15 - 1).\n* It is not a cryptographically secure random number generator (CSPRNG), so it's not suitable for <mark>security-related purposes</mark>.\n* If higher precision or security is required, it's recommended to use `/dev/urandom`.\n","lang":"en"},"__N_SSG":true}